# Render Blueprint — deploy CAPCUT Easy CUT อัตโนมัติ
# วิธีใช้: Render Dashboard -> New -> Blueprint -> เลือก repo นี้ -> Apply
# แล้วไปใส่ค่า Environment ที่ sync:false (คีย์ลับ) ในหน้า service เอง
services:
  - type: web
    name: capcut-easy-cut
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: singapore
    plan: free             # เริ่มฟรีก่อน (ไม่มีค่าใช้จ่าย) — อัปเป็น starter ทีหลังถ้าเรนเดอร์วิดีโอหน่วง
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: GROQ_API_KEY            # ถอดเสียงบนคลาวด์ (จำเป็น — ไม่มี torch บนเซิร์ฟเวอร์)
        sync: false
      - key: SUPABASE_URL            # ฐานข้อมูลสมาชิก/โควตา (จำเป็นบนคลาวด์ ไม่งั้นข้อมูลหายตอน deploy)
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
      - key: EASYCUT_PROMPTPAY_ID    # เบอร์พร้อมเพย์รับเงินสมาชิก
        sync: false
      - key: EASYCUT_PROMPTPAY_NAME  # ชื่อบัญชีพร้อมเพย์
        sync: false
      - key: RESEND_API_KEY          # ส่งอีเมล OTP ผ่าน HTTPS API (แนะนำบน Render)
        sync: false
      - key: BREVO_API_KEY           # ส่ง OTP ผ่าน Brevo HTTPS API ไปยังผู้ใช้ทั่วไป
        sync: false
      - key: BREVO_SENDER_EMAIL
        sync: false
      - key: EASYCUT_MAIL_FROM       # เช่น CAPCUT Easy CUT <verify@yourdomain.com>
        sync: false
      - key: ADMIN_EMAIL             # ชื่อสำหรับปลดล็อกคำสั่งสำคัญในหลังบ้าน
        sync: false
      - key: ADMIN_PASSWORD          # เก็บเป็น secret ใน Render เท่านั้น
        sync: false
      - key: EASYCUT_SMTP_HOST       # ส่งอีเมล OTP (ไม่บังคับ)
        sync: false
      - key: EASYCUT_SMTP_USER
        sync: false
      - key: EASYCUT_SMTP_PASS
        sync: false
