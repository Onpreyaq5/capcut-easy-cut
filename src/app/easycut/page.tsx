import { EasyCutTool } from '@/components/EasyCutTool';

export const metadata = {
  title: 'CAPCUT Easy CUT — ตัด Dead air + ทำซับอัตโนมัติ',
};

export default function EasyCutPage() {
  return <EasyCutTool />;
}
